{
  "name": "PetSociality",
  "description": "Tu mascota más social que nunca.",
  "version": "0.1.0",
  "dependencies": {
    "bootstrap-sass": "^3.3.6",
    "jquery": "^2.1.4",
    "page": "^1.6.4",
    "qs": "^6.0.0",
    "serve": "^1.4.0"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.1.18",
    "babelify": "^7.2.0",
    "browserify": "^12.0.1"
  },
  "scripts": {
    "public": "mkdir -p public",
    "build-js": "NODE_PATH=. browserify -t [ babelify --presets [ es2015 ] ] src/js/app.js > public/js/app.js",
    "copy-files": "cp src/css/app.css public/css/app.css && cp src/index.html public/index.html",
    "build": "npm run public && npm run build-js && npm run copy-files",
    "serve": "serve public"
  }
}
